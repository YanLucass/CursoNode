<h1>Editando: {{user.name}}</h1>

<form action="/user/update" method="POST" class="form">
    <input type="hidden" name="id" value="{{user.id}}">
    <div class="form-control">
        <label for="name">Nome</label>
        <input type="text" name="name" value="{{user.name}}">
    </div>

     <div class="form-control">
        <label for="occupation">Profissão:</label>
        <input type="text" name="occupation" value="{{user.occupation}}">
    </div>

     <div class="form-control form-checkbox">
        {{#if user.newsletter}}
        <input type="checkbox" name="newsletter" checked>
        {{else}}
        <input type="checkbox" name="newsletter">
        {{/if}}
        <label for="newsletter">Enviar promoções?</label>
    </div>

    <input type="submit" value="Editar">
</form>


{{!-- Address --}}

<div class="address-container">

    <h2>Endereço:</h2>
    <form action="/address/create" method="POST">

    {{!-- Nome igual na tabela pro sequelize entender --}}
    <input type="hidden" name="UserId" value="{{user.id}}">

    <div class="form-control">
        <label for="city">Cidade:</label>
        <input type="text" name="city" placeholder="Digite sua cidade">
    </div>

    <div class="form-control">
        <label for="street">Rua:</label>
        <input type="text" name="street" placeholder="Digite sua rua">
    </div>

    <div class="form-control">
        <label for="number">Número</label>
        <input type="text" name="number" placeholder="Digite o numero">
    </div>

    <input type="submit" value="Incluir endereço">
</form>

  
</div>
